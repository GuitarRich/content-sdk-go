# Sitecore Content SDK for Go - Environment Configuration

# === Edge API Configuration (Recommended for Production) ===
# When using Sitecore Edge (cloud-hosted):
USE_EDGE_API=true
SITECORE_EDGE_CONTEXT_ID=your-context-id-here
SITECORE_EDGE_CLIENT_CONTEXT_ID=your-client-context-id-here
SITECORE_EDGE_URL=https://edge.sitecorecloud.io

# === Local API Configuration (Development) ===
# When using local Sitecore instance:
# USE_EDGE_API=false
# SITECORE_API_KEY=your-api-key-here
# SITECORE_API_HOST=https://cm.yourdomain.local

# === Site Configuration ===
DEFAULT_SITE_NAME=your-site-name
DEFAULT_LANGUAGE=en

# === Multisite Configuration ===
MULTISITE_ENABLED=true
MULTISITE_USE_COOKIE=true

# === Personalization (CDP/Personalize) ===
PERSONALIZE_ENABLED=false
PERSONALIZE_SCOPE=your-personalize-scope
CDP_ENDPOINT=https://api.boxever.com

# === Editing/Preview Mode ===
EDITING_ENABLED=false
EDITING_SECRET=your-editing-secret
SITECORE_INTERNAL_EDITING_HOST_URL=http://localhost:3000

# === Timeouts ===
EDGE_TIMEOUT=10s
CDP_TIMEOUT=400ms

# === Debug ===
# Set to enable debug logging (any value enables it)
# DEBUG=1

